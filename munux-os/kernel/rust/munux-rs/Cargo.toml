[package]
name = "munux-rs"
version = "0.3.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
publish.workspace = true
description = "Safe-Rust core of the Munux kernel. no_std, no unsafe outside narrowly justified exceptions."

[lib]
name = "munux_rs"
crate-type = ["rlib"]

[features]
default = []
host-tests = []
